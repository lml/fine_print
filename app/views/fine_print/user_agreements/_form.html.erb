<%= form_for(@user_agreement) do |f| %>
  <% if @user_agreement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_agreement.errors.count, "error") %> prohibited this user_agreement from being saved:</h2>

      <ul>
      <% @user_agreement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :agreement_id %><br />
    <%= f.number_field :agreement_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
